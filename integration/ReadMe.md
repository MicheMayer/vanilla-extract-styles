# Test Interation

This project demonstrates how to use Vanilla Extract Styles Package for styling React components.

## Provide Package locally

To provide the package locally, use the following command in the repository root:

```bash
npm run pack-locally
```